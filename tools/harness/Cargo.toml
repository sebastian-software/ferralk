[package]
name = "harness"
description = "Unpublished corpus validation harness for ferralk development."
version = "1.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish = false

[dependencies]
corpus = { path = "../corpus" }
ferralk = { path = "../..", features = ["unstable-test-hooks"] }
ferralk-glob = { path = "../../crates/ferralk-glob", features = ["unstable-test-hooks"] }
jsonschema.workspace = true
serde_json.workspace = true

[lints]
workspace = true

[dev-dependencies]
fast-glob = "=1.1.1"
