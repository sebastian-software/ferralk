# Development tracks stable; the MSRV is `rust-version` in Cargo.toml and is
# checked by its own CI job. Tracking stable means rustfmt output can shift
# between stable releases; pin a version here if that must not happen.
[toolchain]
channel = "stable"
components = ["clippy", "rustfmt"]
