[package]
name = "ferralk"
description = "Portable filesystem walking for ferralk."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
keywords.workspace = true
categories.workspace = true

[features]
native-linux = []
native-macos = []

[dependencies]
crossbeam-deque = "0.8"
ferralk-glob = { path = "../ferralk-glob", version = "0.3.0" }
memchr = "2.7"

[dev-dependencies]
corpus = { path = "../../tools/corpus" }
# The engine ADR-0014 replaced, kept for the test that compares the two rule
# matchers over generated rules and paths.
ignore = "0.4"
loom = "0.7"
serde_json.workspace = true

[lints]
workspace = true
