{"id":"dotfile-001","pattern":"*","path":".hidden","flags":[],"expected":false,"oracle_expected":true,"source":"handwritten","disputed":true,"note":"ADR-0011: an ordinary wildcard never consumes the period that starts a component. zlob 1.6.3's Rust matchPaths entry point applies no leading-period rule and accepts the candidate; its own frozen Zig filesystem glob excludes it, as path-matcher.jsonl already records. ADR-0011 keeps the POSIX rule as ferralk's primary result."}
{"id":"dotfile-002","pattern":"*","path":".hidden","flags":["match_hidden"],"expected":true,"source":"handwritten","note":"The same pattern accepts the name once leading periods are opted in."}
{"id":"dotfile-003","pattern":".*","path":".hidden","flags":[],"expected":true,"source":"handwritten","note":"A literal period in the pattern matches the leading period without any flag."}
{"id":"dotfile-004","pattern":"?","path":".","flags":[],"expected":false,"oracle_expected":true,"source":"handwritten","disputed":true,"note":"A single-byte wildcard is bound by the same rule as a star. zlob 1.6.3's Rust matchPaths entry point applies no leading-period rule and accepts the candidate; its own frozen Zig filesystem glob excludes it, as path-matcher.jsonl already records. ADR-0011 keeps the POSIX rule as ferralk's primary result."}
{"id":"dotfile-005","pattern":"?","path":".","flags":["match_hidden"],"expected":true,"source":"handwritten","note":"With the flag the wildcard consumes the period like any other byte."}
{"id":"dotfile-006","pattern":"[.]","path":".","flags":[],"expected":false,"oracle_expected":true,"source":"handwritten","disputed":true,"note":"A character class naming the period is still refused at a component start. zlob 1.6.3's Rust matchPaths entry point applies no leading-period rule and accepts the candidate; its own frozen Zig filesystem glob excludes it, as path-matcher.jsonl already records. ADR-0011 keeps the POSIX rule as ferralk's primary result."}
{"id":"dotfile-007","pattern":"[.]","path":".","flags":["match_hidden"],"expected":true,"source":"handwritten","note":"The class matches once the leading-period rule is lifted."}
{"id":"dotfile-008","pattern":"*.txt","path":".notes.txt","flags":[],"expected":false,"oracle_expected":true,"source":"handwritten","disputed":true,"note":"A suffix pattern cannot reach across the leading period. zlob 1.6.3's Rust matchPaths entry point applies no leading-period rule and accepts the candidate; its own frozen Zig filesystem glob excludes it, as path-matcher.jsonl already records. ADR-0011 keeps the POSIX rule as ferralk's primary result."}
{"id":"dotfile-009","pattern":"*.txt","path":".notes.txt","flags":["match_hidden"],"expected":true,"source":"handwritten","note":"The same suffix pattern matches the hidden file with the flag."}
{"id":"dotfile-010","pattern":"a/*","path":"a/.hidden","flags":[],"expected":false,"oracle_expected":true,"source":"handwritten","disputed":true,"note":"The rule applies at every component start, not only the first. zlob 1.6.3's Rust matchPaths entry point applies no leading-period rule and accepts the candidate; its own frozen Zig filesystem glob excludes it, as path-matcher.jsonl already records. ADR-0011 keeps the POSIX rule as ferralk's primary result."}
{"id":"dotfile-011","pattern":"a/*","path":"a/.hidden","flags":["match_hidden"],"expected":true,"source":"handwritten","note":"The flag lifts the rule in every component."}
{"id":"dotfile-012","pattern":"*/b","path":".a/b","flags":[],"expected":false,"oracle_expected":true,"source":"handwritten","disputed":true,"note":"A hidden directory name is protected exactly like a hidden file name. zlob 1.6.3's Rust matchPaths entry point applies no leading-period rule and accepts the candidate; its own frozen Zig filesystem glob excludes it, as path-matcher.jsonl already records. ADR-0011 keeps the POSIX rule as ferralk's primary result."}
{"id":"dotfile-013","pattern":"*/b","path":".a/b","flags":["match_hidden"],"expected":true,"source":"handwritten","note":"The flag lets the wildcard consume a hidden directory name."}
{"id":"dotfile-014","pattern":"**/*.rs","path":".config/main.rs","flags":["recursive_double_star"],"expected":false,"source":"handwritten","note":"Recursive ** does not bypass the rule for the hidden component it crosses."}
{"id":"dotfile-015","pattern":"**/*.rs","path":".config/main.rs","flags":["recursive_double_star","match_hidden"],"expected":true,"source":"handwritten","note":"With the flag the recursive form reaches inside the hidden directory."}
{"id":"dotfile-016","pattern":"a/*/c","path":"a/.b/c","flags":[],"expected":false,"oracle_expected":true,"source":"handwritten","disputed":true,"note":"An intermediate hidden component blocks the match. zlob 1.6.3's Rust matchPaths entry point applies no leading-period rule and accepts the candidate; its own frozen Zig filesystem glob excludes it, as path-matcher.jsonl already records. ADR-0011 keeps the POSIX rule as ferralk's primary result."}
{"id":"dotfile-017","pattern":"a/*/c","path":"a/.b/c","flags":["match_hidden"],"expected":true,"source":"handwritten","note":"The flag admits the intermediate hidden component."}
{"id":"dotfile-018","pattern":".*/*","path":".a/b","flags":[],"expected":true,"source":"handwritten","note":"Naming the period explicitly in the first component needs no flag."}
{"id":"dotfile-019","pattern":"*","path":"a.b","flags":[],"expected":true,"source":"handwritten","note":"A period inside a name is an ordinary byte; only a leading one is special."}
{"id":"dotfile-020","pattern":"*","path":"..","flags":[],"expected":false,"oracle_expected":true,"source":"handwritten","disputed":true,"note":"The parent-directory name starts with a period and is protected as well. zlob 1.6.3's Rust matchPaths entry point applies no leading-period rule and accepts the candidate; its own frozen Zig filesystem glob excludes it, as path-matcher.jsonl already records. ADR-0011 keeps the POSIX rule as ferralk's primary result."}
{"id":"dotfile-021","pattern":".?","path":".a","flags":[],"expected":true,"source":"handwritten","note":"The wildcard sits after the literal period, so the rule does not apply to it."}
{"id":"dotfile-022","pattern":"{.a,b}","path":".a","flags":["braces"],"expected":true,"source":"handwritten","note":"A brace alternative may spell out a hidden name, which then matches literally."}
